# Sprint 2 — Frontend base (completado)

## Prompt resumido

```text
Iniciar Sprint 2 frontend.
Expo + TypeScript, arquitectura src/ (components, screens, navigation, services, store, hooks).
React Navigation, Zustand + AsyncStorage, UI Paper.
Login → POST /api/auth/login, sesión persistente, rutas protegidas.
Dashboard inicial con GET /api/harinas.
EXPO_PUBLIC_API_URL para la API. Avanzar por fases, archivo por archivo.
```
